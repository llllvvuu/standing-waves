{
  "scripts": {
    "build": "tsup dist",
    "watch": "tsup --watch",
    "format": "prettier --write src/**/*"
  },
  "devDependencies": {
    "prettier": "^3.0.1",
    "tsup": "^7.1.0"
  },
  "dependencies": {
    "scale-color-perceptual": "^1.1.2"
  }
}
